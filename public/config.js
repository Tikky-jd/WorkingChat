// 前端 API 基地址配置
// ------------------------------------------------------------------
// 当前部署方式：宝塔(Linux) 同域托管——Nginx 在 443 做 HTTPS 终端，
// 静态页与 /api、/uploads 同属一个站点（前端由 Nginx 直接发，接口反代到本地 node）。
// 因为前后端同源，API_BASE 留空即可（请求走相对路径 /api/...）。
window.API_BASE = '';

// 历史/备选方案参考：
// - 仅后端上云、前端仍挂 GitHub Pages（需跨域 + 受信任证书）：
//     window.API_BASE = 'https://你的域名';
// - 本地同源调试（node server.js 顺带托管前端）：留空即可。
